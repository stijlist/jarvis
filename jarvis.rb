class Jarvis
    # loop!!!
    # get queue of more events from zulip
        # loop.do 
        # request = queue.pop
        # parsed_request = Listener.listen(request)
        # bookable = Calendar.bookable_for(parsed_request)?
        # if bookable
            # Calendar.add(parsed_request)
            # response = Added blah
        # else:
            # response = Room is busy at time
        # Zulip.send_response(response)
end